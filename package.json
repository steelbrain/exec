{
  "name": "sb-exec",
  "version": "4.0.0",
  "description": "Process spawning APIs beautified",
  "main": "lib/index.js",
  "scripts": {
    "lint": "(flow check) && (eslint .) && (prettier -l '**/*.js')",
    "test": "apm test",
    "compile": "sb-babel-cli -o lib src",
    "watch": "npm run compile -- --watch"
  },
  "engines": {
    "node": ">=4"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "babel-preset-steelbrain": "^5.0.0",
    "eslint-config-steelbrain": "^5.0.2",
    "flow-bin": "^0.66.0",
    "jasmine-fix": "^1.0.1",
    "sb-babel-cli": "^0.1.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/steelbrain/exec.git"
  },
  "keywords": ["child_process", "process", "exec"],
  "author": "steelbrain",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/steelbrain/exec/issues"
  },
  "homepage": "https://github.com/steelbrain/exec#readme",
  "dependencies": {
    "consistent-env": "^1.2.0",
    "lodash.uniq": "^4.5.0",
    "sb-npm-path": "^2.0.0"
  }
}
