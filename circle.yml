dependencies:
  override:
    - wget -qO- https://git.io/vPmPy | sh # Install Atom
    - wget -qO- https://git.io/vPm1q | sh # Debug Versions
    - wget -qO- https://git.io/vPZfC | sh # NPM Install

test:
  override:
    - npm run lint
    - npm test

machine:
  node:
    version: 4
